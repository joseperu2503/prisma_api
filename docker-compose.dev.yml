services:
  api:
    container_name: prisma_api_${ENV}
    build:
      context: .
      dockerfile: Dockerfile.dev
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules
    ports:
      - '${SERVER_PORT}:${SERVER_PORT}'
    depends_on:
      - db
    image: prisma_api_${ENV}
    networks:
      - prisma_net
    env_file:
      - .env.${ENV}

  db:
    container_name: ${DB_HOST}
    image: postgres:14.3
    restart: no
    ports:
      - '${DB_EXPOSED_PORT}:${DB_PORT}'
    networks:
      - prisma_net
    environment:
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
      PGPORT: ${DB_PORT}
      TZ: America/Lima
    volumes:
      - ./postgres/${ENV}:/var/lib/postgresql/data

networks:
  prisma_net:
